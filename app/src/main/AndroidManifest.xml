<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.e_canteenapp">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="e-Canteen App"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EcanteenApp">

        <!-- MainActivity -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- RegisterActivity -->
        <activity
            android:name=".RegisterActivity"
            android:exported="true" />

        <!-- MenuActivity -->
        <activity
            android:name=".MenuActivity"
            android:exported="true" />

        <!-- ProfileActivity -->
        <activity
            android:name=".ProfileActivity"
            android:exported="true" />

        <activity
            android:name=".LunchActivity"
            android:exported="true"/>

        <activity android:name=".SnacksActivity" />
        <activity android:name=".SpecialItemActivity" />
        <activity android:name=".BreakfastActivity" />
        <activity android:name=".ReceiptActivity" />
        <activity android:name=".AddEditItemActivity" />
        <activity android:name=".AdminPendingOrdersActivity" />
        <activity android:name=".AdminDeliveredOrdersActivity" />
        <activity android:name=".SpecialItemsAdminActivity" />
        <activity android:name=".AdminNotificationSenderActivity" />
        <activity android:name=".AdminDashboardActivity" />
        <activity android:name=".NotificationActivity" />
        <activity android:name="com.razorpay.CheckoutActivity" />
        <activity android:name=".SpecialReceiptActivity" />
        <activity android:name=".AdminOrderHistoryActivity" />
        <activity android:name=".ForgotPasswordActivity" android:exported="true" />
        <activity android:name=".UserOrderHistoryActivity" />








        <service
            android:name=".MyFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>



    </application>

</manifest>
